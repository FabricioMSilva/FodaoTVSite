# Netlify config for the static frontend site.
# Publish from the repository root because this folder is pushed as a standalone site repo.
[build]
  publish = "."
  command = "echo 'skip build'"

[[redirects]]
  from = "/sw.js"
  to = "/sw.js"
  status = 200
